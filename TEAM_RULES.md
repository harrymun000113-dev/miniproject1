# TEAM_RULES.md
### 협업 규칙 및 AI 수정 규칙

> `BLUE_OCEAN_SCORE.md`, `DATA_SCHEMA.md`가 이 프로젝트의 Single Source of Truth입니다.
> 어떤 AI(GPT, Gemini, Claude, Cursor 등)를 쓰든, 코드나 문서를 수정하기 전에 반드시 이 세 문서를 먼저 읽고 작업하세요.

---

## 0. 왜 문서를 먼저 읽히나요?

팀원마다 서로 다른 AI로 바이브코딩을 합니다. AI는 대화가 새로 시작될 때마다 이 프로젝트를 모르기 때문에, 문서 없이 코드를 요청하면 변수명, 공식, 데이터 흐름이 사람마다 다르게 만들어집니다.

세 문서는 AI에게 다음 세 가지를 알려주는 공통 컨텍스트입니다.

- **맥락**: 무엇을 만드는 서비스인가 (HS CODE 입력 → 한국이 놓친 타깃국 상위 20개)
- **구조**: 데이터가 어떤 순서로 흐르는가 (깔때기: 수집 → 500만 달러 컷 → 간이 점수 → 상위 50 → 정밀 점수 → 상위 20)
- **규칙**: 무엇을 바꾸면 안 되는가 (변수명, 100점 배점, 깔때기 규칙)

이 세 가지를 이해한 상태에서 자기 담당 파일에 어울리는 코드를 쓰게 하는 것이 목적입니다.

---

## 1. 팀 협업 절차

AI에게 코드 작성 또는 문서 수정을 요청할 때 다음 순서를 따릅니다.

1. `BLUE_OCEAN_SCORE.md`와 `DATA_SCHEMA.md`를 먼저 읽는다
2. 기존 프로젝트 정의(변수명, 공식, 배점, 깔때기 규칙)를 파악한다
3. 기존 변수명과 계산식을 유지한다
4. 사용자의 추가 요청만 반영한다
5. 기존 정의와 충돌하면 임의로 변경하지 않는다
6. 충돌 내용이 있으면 먼저 팀에 명시하고 협의한다
7. 변경이 확정되면 `BLUE_OCEAN_SCORE.md`의 CHANGELOG 섹션에 기록한다

---

## 2. AI Modification Rules

이 프로젝트 문서/코드를 다루는 AI는 다음 규칙을 따라야 합니다.

### MUST (반드시 지킬 것)
- 현재 문서의 프로젝트 목적을 유지할 것
- 기존 변수명을 유지할 것 (`DATA_SCHEMA.md` 기준)
- 기존 계산 공식과 배점을 기준으로 작업할 것 (`BLUE_OCEAN_SCORE.md` 기준)
- 깔때기 순서를 지킬 것: 500만 달러 초과 국가만 통과 → 간이 점수 → **상위 50개국** → 정밀 수집·점수 → 상위 20개국 출력
- 같은 HS CODE 입력에는 같은 결과가 나오게 할 것 (재현성)
- 기존 데이터 구조를 유지할 것
- 실제 데이터와 가상(예시) 데이터를 명확히 구분할 것
- 수정사항을 명확하게 표시할 것 (어디를, 왜 바꿨는지)

### MUST NOT (하지 말 것)
- 점수 공식이나 배점(시장 규모 15, CAGR 15, 전년 성장률 10, 한국 수출 비중 25, 경쟁 개방성 15, 관세 10, Google 트렌드 5, 환율 5)을 이유 없이 변경하지 말 것
- 후보 선별을 랜덤 샘플링이나 다른 규칙으로 바꾸지 말 것 (상위 50개 단일 규칙)
- 정규화를 소수의 벤치마크 국가를 임의로 추가해서 하지 말 것
- 존재하지 않는 API나 데이터를 존재한다고 가정하지 말 것
- API 실패 시 가상 데이터로 대체하지 말 것, 결측을 임의로 0으로 채우지 말 것
- 가상의 데이터를 실제 데이터라고 표현하지 말 것
- 기존 변수명을 임의로 번역하거나 변경하지 말 것 (예: `korea_market_share` → `한국점유율`로 코드 내 변경 금지)
- 특정 AI만 이해할 수 있는 문법(플러그인 전용 태그 등)을 추가하지 말 것

---

## 3. 파일 소유권 (자기 파일만 수정)

| 담당 | 파일 | 비고 |
|---|---|---|
| A | `src/collect_data_A.py` | Comtrade/K-Stat, 환율, 트렌드, 박람회 데이터 수집, 캐시 |
| B | `src/preprocess_kpi_B.py` | 전처리, KPI, Export Gap, 간이 점수, 상위 50 후보 선별 |
| C | `src/score_engine_C.py` | 100점 스코어(Potential, Blue Ocean Score), 순위 |
| D | `src/insight_D.py` | 경쟁국 분석, 인사이트 문장, 계절성(진출 타이밍) |
| E | `src/dashboard_E.py` | Streamlit 대시보드, 배포 |

다른 사람 파일을 고치고 싶으면 반드시 PR로 요청 후 본인 동의를 받아 병합합니다.

---

## 4. 개발 환경 (버전 고정)

- Python **3.11.x**
- 아래 버전 그대로 사용, 임의 최신 버전 설치 금지

```
streamlit==1.38.0
pandas==2.2.2
numpy==1.26.4
plotly==5.24.1
python-dotenv==1.0.1
requests==2.32.3
comtradeapicall==1.0.4
openpyxl==3.1.5
```

새 라이브러리(예: Google Trends용 `pytrends`, 크롤링용 라이브러리)가 필요하면 팀에 먼저 공유 후 버전과 함께 `requirements.txt`에 추가합니다.

API 키는 `.env`에만 저장하고 커밋하지 않습니다 (`.gitignore`에 `.env`, `data/cache/` 포함).

---

## 5. 코딩 컨벤션

- 변수/함수명은 영문 snake_case만 사용 (한글 변수명 금지)
- 모든 함수는 **pandas DataFrame in → DataFrame out** (dict, list 반환 금지). 마켓 요약처럼 값이 하나인 결과도 1행 DataFrame으로 반환한다 (`DATA_SCHEMA.md` 5-1번)
- 원본 DataFrame을 직접 수정하지 말고 `df.copy()` 후 작업
- 함수 상단에 docstring으로 입출력 형식 명시

---

## 6. Git 규칙

- 브랜치명: `feature/담당자명-기능명` (예: `feature/tony-collect-data`)
- 커밋 메시지: `[담당자] 내용` (예: `[tony] Export Gap 계산 함수 추가`)
- `main` 브랜치 직접 push 금지, PR로만 병합
- 하루 작업 끝나면 당일 커밋+push
- 점수/배점/깔때기에 영향을 주는 PR은 `BLUE_OCEAN_SCORE.md`의 CHANGELOG 갱신 여부를 PR 본문에 적는다

---

## 7. AI에게 작업 요청 시 프롬프트 템플릿

```
너는 이 프로젝트의 BLUE_OCEAN_SCORE.md와 DATA_SCHEMA.md를 기준으로 작업해야 해.
아래 파일 내용을 먼저 읽고, 여기 정의된 변수명·공식·배점·깔때기 규칙을 절대 임의로 바꾸지 마.

[BLUE_OCEAN_SCORE.md 내용 붙여넣기]
[DATA_SCHEMA.md 내용 붙여넣기]
[TEAM_RULES.md 내용 붙여넣기]

프로젝트 요약: HS CODE를 입력하면 전 세계 수입국을 분석해, 시장 수요와 성장성은 높지만
한국산 침투율은 낮은 상위 20개 타깃국을 보여주는 서비스야. 데이터 흐름은 깔때기 방식
(500만 달러 초과 국가 → 간이 점수 → 상위 50개국 → 정밀 수집·100점 점수 → 상위 20개국)이야.

Python 3.11, pandas 2.2.2, streamlit 1.38.0 기준 문법으로 작성해줘.
모든 함수는 pandas DataFrame을 받아서 DataFrame을 반환해야 해.
변수명은 영문 snake_case만 쓰고, 위 문서의 컬럼명과 정확히 일치시켜줘.

내 담당 파일은 [파일명] 하나야. 다른 모듈 파일은 건드리지 마.
기존 정의와 충돌하는 부분이 있으면 마음대로 고치지 말고 먼저 나에게 알려줘.
실제 API 응답이 실패하면 가상 데이터로 대체하지 말고 오류를 그대로 알려줘.
```

### 작업 유형별로 덧붙이는 문장

**기능 구현**
```
[작업] <구현할 기능>
[입력] <DataFrame 컬럼>  [출력] <DataFrame 컬럼>
[관련 문서 섹션] 예: BLUE_OCEAN_SCORE.md 4번
[완료 조건] <검증 방법, 예: 같은 HS CODE를 2번 실행해 결과가 동일>
```

**버그 수정**
```
[증상] <화면/로그에서 본 현상>
[재현] HS CODE=<...>
[기대 결과] <문서 기준으로 어떻게 나와야 하는지>
[실제 결과] <출력 일부>
```

---

## 8. 통합 전 체크리스트 (Day 7 통합 전 각자 확인)

- [ ] 내 코드가 `DATA_SCHEMA.md`의 컬럼명을 정확히 그대로 입출력하는지 확인
- [ ] 내 함수가 `BLUE_OCEAN_SCORE.md`의 공식·배점과 일치하는지 확인 (배점 합계 100)
- [ ] 같은 HS CODE를 2번 실행했을 때 결과가 동일한지 확인
- [ ] 성장률(`growth_1y`, `cagr_3y`)이 대부분의 국가에서 결측이 아닌지 확인 (다년도 수집)
- [ ] 한국 점유율이 작은 국가가 화면에서 0으로 보이지 않는지, 결측이 0으로 표시되지 않는지 확인
- [ ] 랭킹 상위 20개가 후보 50개국 안에서 나왔는지 확인
- [ ] `python src/내파일.py` 단독 실행 시 에러 없는지 확인
- [ ] 새로 추가한 라이브러리가 있으면 팀에 공유했는지 확인
- [ ] 가상 데이터로 만든 부분과 실제 API 연동 부분을 주석으로 구분했는지 확인

---

## 9. 충돌 발생 시 대응 원칙

- 컬럼명/공식이 서로 다르게 구현된 경우 → **`DATA_SCHEMA.md`와 `BLUE_OCEAN_SCORE.md`가 항상 기준.** 다른 쪽 코드를 문서에 맞게 고친다
- 두 사람이 같은 파일을 동시에 수정해 git 충돌(merge conflict)이 난 경우 → 임의로 아무거나 선택하지 말고 직접 협의해서 어느 로직을 살릴지 결정
- AI가 생성한 코드가 이 문서 규칙과 다르게 나왔다면 → 그대로 커밋하지 말고 "문서 기준에 맞게 다시 짜줘"라고 재요청
- 공식/배점을 변경해야 할 필요가 생기면 → 임의로 바꾸지 말고 `BLUE_OCEAN_SCORE.md`의 개발원칙(13번)과 CHANGELOG(14번) 절차를 따를 것

---

## 10. 미확정 결정 사항 (확정되면 해당 문서에 반영 후 삭제)

| 항목 | 담당 | 상태 |
|---|---|---|
| 주 데이터 소스 (Comtrade vs K-Stat) | | 미정 |
| Comtrade 무료 티어의 다중 국가 일괄 조회 가능 여부, 호출 한도 재확인 | A | 확인 필요 |
| `tariff_rate` 소스 (점수용 검증 가능 데이터 확보) | | 미정 |
| `fx_change_3y` 정의 (통화쌍, 방향) | | 초안, 확정 필요 |
| Google Trends 방식 (pytrends vs SerpAPI) | A | 미정 |
| `underpenetrated_flag`를 순위 필수 필터로 쓸지 | | 미정 |
| 극단값(winsorize) 처리 여부 | B, C | 미정 |
| 박람회 크롤링 대상 사이트, `hs_industry_map` 구축 | A, D | 미정 |
| 간이 점수 검증 (상위 20 재현율 ≥ 0.9) | B | 개발 후 수행 |
